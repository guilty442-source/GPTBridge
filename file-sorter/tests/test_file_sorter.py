"""file-sorter self-health test suite (A57/E43).

Single managed test file for the file-sorter module.
Collected by the maintenance sovereign via SELF_HEALTH_MANAGED_TEST_FILES.

Test stubs — implementation pending.
"""
from __future__ import annotations

import pytest


@pytest.mark.skip(reason="skeleton — implementation pending")
def test_file_sorter_contract() -> None:
    """Verify file-sorter service contract and governance boundary."""


@pytest.mark.skip(reason="skeleton — implementation pending")
def test_file_sorter_cleanup_offline() -> None:
    """Verify file-sorter cleanup works without network dependency."""


@pytest.mark.skip(reason="skeleton — implementation pending")
def test_file_sorter_metadata_v2() -> None:
    """Verify file-sorter metadata v2 contract."""
