// parser.cpp — parsing compute core (A221/E186).
//
// Owns parsing compute (token estimation, text analysis).
// Implementation will be added when parsing compute native acceleration
// is required (A218/E183: deterministic high-load threshold dispatch).
//
// Private .hpp headers may be added here when required by this
// implementation (A221: remain inside native/core, never application-facing).
