# Qwen3 reranker runtime. Vector and keyword retrieval are local-only
# (sqlite3 hash semantic index + local keyword store, no Qdrant/PostgreSQL).
sentence-transformers>=4.1.0
transformers>=4.51.0
psutil>=6.0,<8
