# 星澄原生模型 + RAG 執行期依賴。
# torch 為原生 Transformer 計算後端；triton 為可選 GPU 加速核心，
# 不可用時自動回退至 PyTorch 純實作。
beautifulsoup4>=4.12,<5
numpy>=1.26,<3
psycopg[binary]>=3.2,<4
qdrant-client>=1.12,<2
psutil>=6.0,<8
sentence-transformers>=4.1.0
tokenizers>=0.20,<1
torch>=2.2,<3

# 可選 GPU 加速（triton 不可用時自動回退至 PyTorch）
# Linux 用官方 triton；Windows 用 triton-windows port（提供同名 triton 模組）
# triton>=3.0,<4 ; sys_platform == "linux"
# triton-windows>=3.0,<4 ; sys_platform == "win32"
