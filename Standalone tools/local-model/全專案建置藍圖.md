# GPTBridge 全專案建置藍圖

> 本檔只保留工作者規則、current 目標與未完成施工。
> 法典是唯一規範權威；本檔不是法典，不建立第二權威。
> 完成紀錄、歷史事件與證據留在 Git、registry 與審計報告，不留在本檔。

## 1. 工作者規則

1. 全專案只使用本檔，不建立第二份藍圖、roadmap、分期表或流程規劃文件。
2. Codex 親自負責法典實作、藍圖編寫、全自動模式腳本及其他程式的唯讀實作差異比對，不得將實作比對交給其他工作者。Codex 不直接實作其他程式；比對後保留較佳且符合現行法典的實作，並更新法典、藍圖或全自動模式規則，不得為符合舊規劃而回退較佳實作。
3. 工作者認領時標示 `[處理中 @worker UTC]`；其他工作者不得修改同一項。超過 24 小時無更新才可重新認領。
4. 工作者以架構與功能施工為主；只有本藍圖明列的 parity、release 觀察窗、資源量測與收斂證據屬施工必要工作，不得擴張成全面測試或重複驗收。
5. 發現工作包與強制契約直接衝突時停止並回報，不自行改法或擴張 authority。
6. 實作優於舊規劃時保留已驗證實作，更新本檔的目標與殘項，不回退較佳實作。
7. 不建立 compat、shim 或臨時平行實作；遷移採 `shadow → parity → primary → retire`，完成後移除舊 owner。
8. 每次工作者提交或本檔修改後，指定法典工作者自動比對 current 程式、設定、registry 與 revision：
   - 施工完成：刪除工作項。
   - 部分完成：只保留可執行殘項。
   - 完成標示與實作不一致：撤銷完成標示。
9. 工作區可能有其他工作者變更；只修改自身檔案，不覆蓋、不重置、不順帶提交他人內容。
10. 未完成清單為空時仍須保留本藍圖；不得因暫無待辦事項刪除、改名或建立替代文件。
11. 未完成清單固定由 `P0` 開始連續編號；高優先度排在前方，同優先度維持依賴順序。新增、刪除或調整優先度後自動排序並重新編號，不保留舊編號或缺口。

## 2. 目標

主要目標：讓 C11 運行核心成為真正的常駐主體，逐步釋放 Python 的機械性工作，同時完整保留治理與安全語意。五核心語言責任及現行法典是唯一架構依據，取消舊版 Python／C# 混合雙宿主假設；架構目標不得誤報為已完成遷移。

### 2.1 五核心與語言責任

| 核心 | 正式責任分配 | 現階段應優先完成 |
| --- | --- | --- |
| 裁決核心 | C 決定性執行、C# 流程、Python 治理語意及必要最終裁決 | C shadow 與 Python 正典結果同位 |
| 權限核心 | C 權限熱路徑、C++ Audit Engine、C# 編排、Python 目錄 | 原生規則索引、授權與稽核閉環 |
| 運行核心 | C11 常駐主體 | 8 個 shadow 元件逐項取得 primary 資格 |
| 自動化核心 | C# 主體＋C primitive | Python automation 工作逐項外移 |
| 星澄助理 | TypeScript UI＋C# 後端 | 全域合規稽查與許可介面契約一致 |

- Python 保留已核定的治理語意與必要最終裁決；C 負責決定性規則與權限熱路徑，C++ 負責原生審計，C# 負責已授權流程編排。
- C 不得建立或修改治理規則；C# 不得繞過裁決、權限或擴張授權；原生規則索引只可投影 canonical permission directory。
- 禁止新增第二套 scheduler、orchestrator、IPC client、治理 authority 或權限來源。

- 星澄是獨立原生模型，不是核心、主宰或獨立工具。
- 星澄人格、自動修復、自我學習、自動編程與內部自我升級屬星澄能力。
- 星澄助理與星澄使用獨立身分組與獨立資料庫，不得混用或互相產生權限。
- 自動修復由星澄提出方案並執行受管流程；無法確定可修復時不得硬修、覆蓋或重置。
- 系統自動更新與自動修復開關留在星澄助理，支援單項許可；星澄自我學習與內部升級不設使用者開關，但仍受治理與資源閘約束。

### 2.2 Runtime 與模組

timer、polling、watcher、queue、scheduler、process lifecycle、health probe、檔案掃描、Git 協調、SQL maintenance、transport pump、test orchestration、已核准 audit check、cleanup、retention、reconciliation 與格式轉換，正式路徑由 C／C++／C# 或資料庫原生能力承載。

Python 只保留治理語意、最終裁決、法典工具、模型訓練及尚未完成 parity 的按需 adapter；完成即釋放。任何過渡 adapter 必須有 owner、替代目標、量測與退役條件。不得為移除 Python 而轉移 authority、破壞 fail-closed 或重寫尚未證明等價的治理語意。

- 持續減少 Python 套件、常駐程序、背景迴圈、中介層與重複實作；機械性能力優先收斂至既有 C11、C++、C# 或資料庫原生能力，不以另一套包裝層取代。
- 每個核心、模組、工具及 worker plane 必須登錄 CPU、記憶體、程序、執行緒、queue、deadline 與並行數硬上限；禁止 `unlimited`、空值或僅告警不限制。
- `CORE_BUDGET_CAP=5` 是核心工作量聚合上限，不得被子程序、工作者或語言 runtime 分拆繞過；資源計算涵蓋 GPTBridge 完整程序樹，不得只計單一 Python 程序。
- 達到上限時依 capability 執行拒絕、排隊、降級、釋放或停止，不得超額執行、隱藏資源占用或以提高上限掩蓋問題。

- 模組是唯一實際工作單位；核心不直接執行領域工作。
- 每模組單一能力、單一職責、按需啟動、版本化契約、有界資源、執行租約、receipt 與可回復生命週期。
- 執行模組以 C／C++／C# 為主；UI 使用 TypeScript；SQL 只做集合式資料運算。
- 模組規則由模組維護，權限核心集中索引；不是獨立權威，也不得進 Runtime 法典規則索引。
- 退役模組標記為垃圾，經星澄助理分類後由自動清理排程處理。

### 2.3 七個獨立工具

1. 投資管家
2. 外部協作
3. 自動化檔案管理
4. 投資管家手機版
5. 本地模型
6. 影音下載自動化
7. 模型對話

清單外不得再宣告獨立工具；星澄及星澄助理都不是獨立工具。各工具啟動與關閉須在 5 秒內；本地模型預設不啟動並提供啟停開關；模型對話自動路由預設星澄優先。

### 2.4 資料與檢索權威

- PostgreSQL：中央 canonical structured data、shared transport、central audit 的唯一 SQL authority。
- SQLite：owner-private operational state，`canonical=false`、禁止跨模組 authority、shared audit 或 permission authority；只允許有界降級並強制 reconciliation。
- Qdrant：語意索引，不取得 canonical authority；每次正式結果須回 PostgreSQL 驗證 scope、revision 與 locator。
- Git：程式碼與交付歷史；不取代 SQL 資料權威。
- 備份、快取、向量與模型輸出都不因存在而取得 authority。

```text
Caller
→ Information Channel
→ Permission / Scope
→ RagApplicationService
→ DAG Planner / Executor
→ CAG Gate
→ RAG Retrieval
→ Evidence Fusion
→ Reranker
→ Context Builder
→ Local LLM
→ Citation Validation
→ Result
```

- DAG 是 orchestration plane，不取代 Application Service。
- CAG 是有版本、有範圍的 acceleration/context reuse plane，不取代 RAG。
- RAG 是 canonical retrieval plane，保留 Hybrid／Code／Memory／Agentic 四子架構。
- PostgreSQL／Qdrant canonical 邊界不變；SQLite 仍只可 bounded degraded fallback。

### 2.5 依賴原則

優先標準函式庫、既有自研核心與作業系統 API。新增第三方套件必須證明標準能力不足、沒有既有 owner、資源與供應鏈成本可接受且有替代／移除路徑。密碼、資料庫協定、模型格式與作業系統安全能力不得為追求零依賴而自行重造。禁止第二套 scheduler、orchestrator、audit policy、governance authority 或 IPC client。

### 2.6 Git 收斂

1. worker 只提交自身明確路徑，永不 push；coordinator 是唯一整合與推送者。
2. 每個 worktree／HEAD generation 只建立一次共享 snapshot；status、diff、changed paths、merge-base、ahead／behind 與 affected scope 供所有 gate 共用。
3. dirty、HEAD、queue 與檔案事件驅動增量處理；週期掃描只作低頻保險。
4. 同一任務的穩定變更合併為一個語意提交；禁止每個小編輯或報告刷新各自提交。
5. 依 commit、受影響路徑與契約版本只處理受影響範圍。
6. generated runtime state、cache、暫存與非 canonical 重跑報告不得進 Git。
7. coordinator 批次整合後只推送一次；無新 commit 或同 generation 已同步時不得重做 fetch／merge／push。
8. 不得削弱 path scope、衝突停止、non-fast-forward／force-push 禁止或來源 revision 約束。

### 2.7 SQL 效能與治理

1. 依 interactive／transport／audit／reconciliation／maintenance／migration 分開量測 query count、round trips、p50／p95／p99、pool wait、lock wait、transaction age、rows scanned／returned、buffer hit、temp spill、WAL、CPU 與 I/O。
2. 優先移除 N+1、重複 lookup、`SELECT *`、無界 result、深 offset 與應用層逐列運算；改用明確欄位、set-based SQL、keyset pagination、prepared statement、bounded batch 及合規的 `COPY`。
3. 索引必須有真實 workload 與 plan 證據；檢查 join order、估算偏差、spill、sequential scan、unused／duplicate index。禁止無量測加索引。
4. 維持 workload lane 隔離；減少短連線、縮短 transaction、禁止 idle-in-transaction、統一 lock order，把網路／模型／檔案工作移出 SQL transaction。
5. 寫入採批次、idempotency key、避免無變更 update；不得停用 RLS、audit、ordering、generation fence 或 write provenance。
6. RAG scope／revision／metadata filter 儘早下推 PostgreSQL，chunk／locator 批次回查，避免每個 Qdrant point 一次 SQL。
7. SQLite 只優化 owner-private fallback；禁止以 SQLite 分流中央 authority 來製造表面加速。
8. partition、materialized view、denormalization、壓縮或 schema 變更必須走 migration、drift gate、RLS parity、回復與重建驗證。

## 3. 未完成清單

| 編號 | 優先度 | 未完成施工 |
| --- | --- | --- |
| P0 | 高 | 更新 epoch seal，完成外部簽章、certification 與最終稽核 |
| P1 | 高 | 補齊 formal rule mapping 與 semantic hash canonical 規格，完成 active transition `[法典工作者收斂 @codex-worker 2026-09-23T07:40Z — formal-rule parity 61/64：FR-XINGCHENG-AUTONOMOUS-TRAINING-UPGRADE／FR-SOVEREIGN-TO-CORE-ENGINE／FR-NO-SUB-SOVEREIGN-ALL-MODULES 仍錨定已 superseded 之 A554／A591／A592，需改錨 A598／A604；A609 重述掃描 15 筆中 A5／E2、A29／E16 未解析（A5 已由 A602 取代，疑掃描器未排除非現行條文）；證據 codex-implementation-convergence-20260923]` |
| P2 | 高 | 閉合 Machine Schema、Directory、implementation obligation 與 final convergence evidence；未閉合不得 verified release `[處理中 @muse-spark 2026-09-23T05:35Z]` `[Codex 實作比對 2026-09-23 — Machine Schema 74 筆已在 staged candidate 轉為 VERIFIED 且零驗證錯誤，但 live Codex 尚未發布；implementation obligations 與其餘 closure 仍未完成。舊藍圖路徑契約問題已修，不再列為殘項]` |
| P3 | 高 | 以 `native-shadow.json` 閉合八個 shadow 元件的輸入輸出、狀態轉移、錯誤、取消、deadline、資源預算與 fail-closed parity；G90／G91 必須與 canonical permission directory 一致 `[處理中 @devin-cli 2026-09-23T06:20Z — 接線稽核完成：8/8 harness 全接線；資源預算觀測已補齊 f551a6d5；殘：ipc 需 Python 請求生命週期表、maintenance cancel 為 Python no-op、runtime_state/ipc deadline 維度]` `[devin-cli 2026-09-23T09:40Z — 四缺口閉合 p4-shadow-gap-closure-20260923b：request_registry 時間戳＋deadline（C created/started/completed/timeout_ms＋binding＋shadow 比較）、scheduler timeout 死碼改實測 timespec_get（error_count 生效）、maintenance cancel 持久化鏈路（with_status→scheduler→controller→observe_cancelled）、runtime_state staleness（C is_stale＋UTC 安全解析＋observe_staleness）；outbox cancel＝unregister 已覆蓋、activation cancel 屬 asyncio 生命週期非狀態維度；native 150/0/0、py shadow 測試 70/70]` `[法典工作者驗證 @codex-worker 2026-09-23T07:15Z — coverage_matrix 部分格過時（watchdog state／deadline 實已比較）；resource 觀測碼已接 8/8 但 *.resources.jsonl 零記錄、僅 model-service-activation 2 筆未解 divergence；真實功能缺口：ipc 請求表、maintenance cancel 持久化、request_registry deadline（C 時間戳為 0）、runtime_state staleness、scheduler timeout dead code；G90／G91 零混用 PASS、C 無權限索引（無第二權威）；殘：索引未對 canonical permission directory 身分做建置期交叉驗證]` `[devin-cli 2026-09-23T10:20Z — 建置期身分交叉驗證落地：build-module-rule-index 現對 identity_directory.db 做 ro 交叉驗證（17 身分 backed、contradiction fail-closed）；runtime _validate 鏡像矛盾檢查；記錄分歧：unbound_paths(worker-plane/native-compute-core/business-logic-csharp)、owner 域 xingcheng-domain 無 sovereign 錨點、27 內部元件未註冊為目錄身分——三者屬註冊表資料裁定，待 codex-worker]` |
| P4 | 高 | 建立逐元件 shadow→primary 證據，涵蓋拒絕授權、無效契約、缺失資料、逾時、損毀、重啟與故障恢復；未完成兩個合規 release 觀察窗者維持 shadow `[處理中 @codex-worker 2026-09-23T07:10Z — 升級證據狀態 p5-shadow-promotion-evidence-status-20260923：8 元件×7 故障模式全 PENDING；release 觀察窗 0/2（A572 未定窗數；專案計畫 ≥2 release 週期；module-language 文件 ≥1 週期之落差待總督裁定）；7/8 元件有未閉 parity 維度＋2 筆未解 divergence；未達標一律維持 shadow，不得自動升級]` |
| P5 | 高 | 盤點 Python 的 timer、polling、queue、scheduler、Git automation、SQL 編排、transport 與 test orchestration，依五核心權責遷至既有 C11、C++、C# 或 DB engine `[處理中 @devin-cli 2026-09-23T06:05Z — 盤點完成 b140c392：23 常駐面分類、8 shadow 在途、3 項受同儕認領閘控、12 面待遷；test-orchestration 已由 C# 獨佔]` `[法典工作者核對 @codex-worker 2026-09-23T07:15Z — 清單漏 14 項常駐面（boot_core supervise／health／watchdog、toolbox reconcile 30s、sleep_policy 60s、maintenance lifecycle／repair、tool-isolation-health、status push、transport notify listener、backend gateway、loop-stall watchdog、toolbox_execution claim poll、permission automation fallback loops）；daily_global_cleaner 與 main_system_self_maintenance 無生產啟動點（幽靈項，先裁定接線或退役）；無新增第二 scheduler／orchestrator／IPC client]` |
| P6 | 高 | 保留 Python 治理語意與必要最終裁決，確認 C／C++／C# 路徑未繞過治理、權限、完整稽核或 fail-closed `[處理中 @devin-cli 2026-09-23T06:50Z]` `[法典工作者驗證 @codex-worker 2026-09-23T07:15Z — b30888d1 為危險能力靜態掃描，CLEAN 僅限該範圍；殘：ToolHost::start 注入 config／hook 可繞 bootstrap env 閘（tool_host.cpp:251-281 vs 332-345）、ToolHost／ChannelRuntime 無 dual-track flag、main-system IPC 審計 fail-open（server_commands.py:96-118）與 shared-layer fail-closed 不一致、audit-engine.exe 僅 mtime 無 digest、26-29 項仍委派 Python]` |
| P7 | 高 | 統一 `CORE_BUDGET_CAP=5` 與 worker-plane 聚合上限的作用範圍；將目前預設關閉的 Job CPU 硬頂改為正式受管上限，補齊 RAM、程序、執行緒、queue、deadline 與並行數硬限制，並涵蓋完整程序樹。現況只有 Python pool cap、10%／30% 軟帳與各工具部分 CPU／memory 設定，RAM 聚合硬頂及全樹資源 p95／p99、shutdown、failure recovery 仍缺 `[Codex 實作比對 2026-09-23]` `[處理中 @devin-cli 2026-09-23T10:05Z — 聚合 Job 硬頂落地 638a97c9：worker_job_cap 轉正式受管預設開（rules JSON 可 opt-out），共享 Job 增 JobMemoryLimit（預設整機 RAM 30%≈20.4 GB）＋ActiveProcessLimit（96），子行程出生即繼承；殘：執行緒/queue/deadline/並行數逐模組硬頂與全樹 p95/p99 量測待 INT-10 窗口後]` |
| P8 | 高 | 完成單元制實體搬移、跨單元介面強制及單元／模組隔離 `[處理中 @devin-cli 2026-09-23T07:30Z — 增2：versioning 機械層下移 shared_layer.versioning，channel_runtime 上行匯入已除；殘 failover_store 惰性 ReconcileService（runtime 服務不可下移）；增1：跨單元匯入邊界稽核器 unit_boundary_check.py 交付（unit-boundary-report.json）：8 條未宣告跨單元邊；實質發現 shared-layer→boot-core 上行依賴（channel_runtime versioning 硬匯入、failover_store ReconcileService 惰性匯入）]` `[devin-cli 2026-09-23T10:40Z — 增3：8→0 未宣告跨單元邊。6 工具 versioning 改接 shared_layer.registry.versioning；failover_store ReconcileService 改注入式 register_reconcile_service_factory（決策面依稽核規則留 core_system）；local-model→boot-core RAG 依賴入 registry 宣告；檢查器修同名模組歧義；unit-migration-map.json 產出 40 元件現況→blocks/ 目標根對照（0 已遷）；實體搬移屬全專案路徑協調窗，非單一工作者可執行]` |
| P9 | 一般 | 完成 C++ 推論生產啟用、runtime switch、batch>1 生產呼叫者與生產 `.pyd` 接線 `[處理中 @devin-cli 2026-09-23T05:33Z — batch>1 全鏈路（wrapper+router+/v1/infer）＋decode UTF-8 sanitize 修復＋.pyd 接線確認；殘：cpp_runtime 生產啟用 flip 待 INT-10 窗口後]` |
| P10 | 一般 | C# 業務編排取得正式生命週期主導權並完成移轉演練 `[處理中 @devin-cli 2026-09-23T05:58Z]` |
| P11 | 一般 | MS6 C++／C# 承接完成，熱路徑移除 Python 中介 |
| P12 | 一般 | 執行 A528／A538 staged amendment，完成外部協作法典同步 `[法典工作者收斂 @codex-worker 2026-09-23T07:40Z — capability-dispatch amendment request 之 change_class=registry-materialization 不在有效類別集（editorial／clarification／provision-scope／authority-duty／architecture-authority／complete-reconstitution），送總督前需重新分類]` |
| P13 | 一般 | 操作者清除 User env `GPTBRIDGE_POSTGRES_*_DSN` |
| P14 | 一般 | 建立 Git 每 generation 共享 snapshot、事件驅動、affected-scope 處理、語意提交批次與單次整合推送 `[處理中 @devin-cli 2026-09-23T04:53Z — snapshot+affected-scope+事件失效 a5c8961a、queue 事件驅動 sync 35abb328、sync dirty 閘走 snapshot 12d22fe9；殘：非-git 寫入 fs watcher、per-scope 提交拆分（授權）、整合推送（協調者）]` |
| P15 | 一般 | 修正 SQL 的 N+1、round trip、無界查詢、深分頁、長交易、索引／統計／vacuum lag 與 RAG SQL 回查 `[處理中 @devin-cli 2026-09-23T05:07Z]` |
| P16 | 一般 | 正式規模語料治理、32k／64k 詞表配比及可重現預訓練報告 `[處理中 @devin-cli 2026-09-23T10:20Z — 配比分析落地 vocab_ratio.py→vocab-ratio-20260923.json（4 tokenizer 全量測於 corpus-v3 dd637a243ed9：8k 3.61→32k 4.03→49k 4.12→64k 4.13 chars/token，zh-TW 64k +22% 最大增益）；可重現預訓練證據 pretrain-reproducibility-20260923.json：pretrain.py 補 torch.manual_seed（init RNG 先前未 seed 為真缺陷），同 seed 雙跑 loss 軌跡與 39 tensor state_sha256 位元相同；corpus-v3 重建 1949 docs/16.3M chars；殘：正式規模語料擴充待授權裁定]` |
| P17 | 一般 | 繁中語料擴充後重跑未見提示探針 |
| P18 | 一般 | 對話品質啟用裁決、現役模型 OOD 改善與啟用後重認證 |
| P19 | 一般 | 完成 MoE、量化、GPU 路由與 xlarge 6 GB 資源邊界施工 `[devin-cli 2026-09-23T10:35Z — 四項齊備已驗證 p19-resource-boundary-20260923：MoE configs＋router-health 證據、int8/int4 量化入推論路徑＋ledger、GpuCoordinator 全路接線（native_engine／cpp_runtime／training_job_executor）、xlarge vram_budget_mb=6144 硬頂先於協調器生效（dense 1437MB 內、xlarge_moe 8241MB 正確降級 CPU）]` |
| P20 | 一般 | 完成自主訓練續訓、專家課程及必要 amendment `[devin-cli 2026-09-23T10:40Z — 自主訓練續訓已在運行（9-22 週期 data-threshold 觸發→SFT→eval→reject perf 回歸，fail-closed 正確）；專家課程機制已建（maturity 閘 _CURRICULUM_COURSES 5/6/7→sft-dialogue/reasoning/evolution，certified<4 fail-closed）；本次啟用 curriculum_enabled=true（L7 認證→路由 sft-refresh）；殘：必要 amendment 屬法典工作者]` |
| P21 | 一般 | 完成模型對話下達系統修改的授權、執行、法典 amendment、回滾及紀錄流程 |
| P22 | 一般 | 五核心語言責任法典 amendment（E177／E178／E179／E186 successor：C11 常駐主體、C 權限熱路徑、C++ 原生審計、C# 已授權編排、Python 治理語意；取消 Python／C# 雙宿主假設）草稿已備，待五核心稽核與總督簽章後發布 `[處理中 @codex-worker 2026-09-23T07:15Z — codex-amendment-request-five-core-language-20260923；未執行、未動法典]` |
| P23 | 一般 | 法典收斂：補 A609 `codex_normative_convergence_registry` 未解析重述（A5／E2、A29／E16）並將 3 條 formal rule（FR-XINGCHENG-AUTONOMOUS-TRAINING-UPGRADE／FR-SOVEREIGN-TO-CORE-ENGINE／FR-NO-SUB-SOVEREIGN-ALL-MODULES）改錨 A598／A604；受管 amendment，不得直接改庫 `[法典工作者發現 @codex-worker 2026-09-23T07:40Z]` |
| P24 | 一般 | `[處理中 @devin-cli 2026-09-23T08:20Z — csharp-native-process-metrics 已建]` 收斂 Python 與第三方依賴。現行 requirements／lock 仍含 psutil、websockets、beautifulsoup4、numpy、Pillow、sentence-transformers、tokenizers、triton；須確認 consumer 已切至既有原生能力後逐項移除，不得只新增替代實作而保留雙路徑 `[Codex 實作比對 2026-09-23]` |
