import type { ToolDefinition } from './types'

/**
 * Developer mode owns advanced tool runtimes.
 * Standalone applications belong in the application registry.
 */
export const developerToolRegistry: ToolDefinition[] = []
