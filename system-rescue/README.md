# System Rescue

Independent `1.0.0` tool for read-only diagnostics plus rescue audit and log
authority. Centralized per-owner backups belong to Global Cleaner and are
available only through the governed shared layer.

```powershell
python system-rescue/src/main.py --check --json
python system-rescue/src/main.py --repair --json
```
