{
  "version": "star-resource-governor-rules/v1",
  "description": "Per-program resource rules for the Process Lasso-inspired governor tier. Every control feature defaults to ON; disable globally in 'defaults' (or via --no-* CLI flags) and refine per program below. 'exclude' keeps a program unmanaged (monitoring only). cpu_limit_percent is a share of total machine CPU (100 = one full core).",
  "defaults": {
    "probalance": true,
    "cpu_limiter": true,
    "background_mode": true,
    "ecoqos": true,
    "limiter_percent": 10.0,
    "resp_strain_ratio": 1.8,
    "worker_job_cap": true,
    "worker_job_percent": 10.0,
    "worker_job_memory_percent": 30.0,
    "worker_job_process_limit": 96
  },
  "programs": {}
}
