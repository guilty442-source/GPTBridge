# Sovereign physical topology

Each child directory is the single physical identity anchor for one effective
sovereign. Its `manifest.json` owns the listed implementation entrypoints.
Legacy Python import paths remain compatibility entrypoints only.
