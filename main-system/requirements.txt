# Runtime dependencies (main system) — Python ONLY for 治相關、決策與模型訓練 (all mechanical → C/C++/C#)
# Heavy / mechanical deps (psutil/qdrant/websockets/torch etc.) are ON-DEMAND or C-native — NOT installed in resident venv
psycopg[binary,pool]>=3.2,<4

# Test dependencies
pytest==9.1.1
pytest-asyncio==1.4.0

# Build dependencies
pybind11>=2.13,<3
pyinstaller==6.22.2
setuptools>=75,<80

# Local-model / standalone-tool dependencies — ON-DEMAND ONLY (not resident)
# Install only when needed: `pip install -e .[local-model]` / `uv pip install -e .[local-model]`
imageio-ffmpeg==0.6.0
numpy>=1.26,<3
Pillow>=10.0,<14
sentence-transformers>=4.1.0
torch>=2.2,<3
