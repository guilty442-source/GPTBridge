{
  "schema": "convergence-evidence/v1",
  "evidence_id": "g99-blocked-classification-20260923",
  "recorded_at_utc": "2026-09-23T04:29:17Z",
  "item": "G99 BLOCKED 測試分類與閘門影響（P0）",
  "changes": [
    "native/test_suites/suite_criticality.json (new): per-suite criticality registry, all 20 canonical suites registered release-critical with affected_capability / release_impact / surfaces; unregistered suites default release-critical (fail-closed); _verify_tool_host_suite excluded as auxiliary (underscore prefix, never in manifest)",
    "push_gate.py: BLOCKED cases now classified per-case into blocked_suite/blocked_case/blocked_reason/required_evidence/affected_capability/release_impact/criticality; release-critical BLOCKED denies push (blocked-release-critical); missing/malformed registry with BLOCKED cases denies (blocked-unclassified); only registry-explicit experimental stays non-blocking INCOMPLETE_EVIDENCE",
    "push_gate.py: bin/suite-manifest.json now required — absent manifest or empty revision treated as stale build output (rebuild under auto_build, deny otherwise); gate record binds source_revision (manifest) + head_revision (git HEAD) + revision_match",
    "csharp/Program.cs (TestSuiteOrchestrator): loads the same registry; --run tags consolidated cases with suite_exe and emits blocked_classifications/blocked_release_critical/blocked_unclassified; verdict FAIL when release-critical blocked or registry missing; aggregate mode classifies via suite_exe (falls back to case suite constant); exit code 1 on deny — verdict parity with the Python gate"
  ],
  "tests": [
    "main-system/tests/test_push_gate.py: 32 passed — new: blocked-release-critical deny, unregistered-suite deny, missing-registry deny, manifest-missing deny, empty-revision deny, source_revision binding, experimental classification fields",
    "pre-existing regression test updated: experimental-only BLOCKED keeps passed=True + incomplete_evidence=True",
    "dotnet build csharp: 0 warnings 0 errors",
    "aggregate mode on real native-report.json (134 PASS/0 FAIL/0 BLOCKED, 20 suites): verdict=PASS, blocked_classifications=[]"
  ],
  "deferred": [
    "real-gate end-to-end run deferred — current bin/ lacks suite-manifest.json so next gate invocation rebuilds once via build.ps1 and emits it; verified via fake-runner pytest path instead (load hygiene: INT-10 window still accumulating)"
  ],
  "notes": "blocked_suite.json in bin/ is a stale artifact from the 2026-09-21 orchestration run (25/0/3); the gate never reads it — reports are only read for suite exes it executes after deleting prior reports. Stale orchestration report (native-orchestration-report.json) is regenerated by --run; the new fields make stale-vs-current distinction explicit via source_revision/manifest_built_revision/revision_match."
}
