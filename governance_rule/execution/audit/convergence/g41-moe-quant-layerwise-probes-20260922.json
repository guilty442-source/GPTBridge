{
  "id": "g41-moe-quant-layerwise-probes",
  "date": "2026-09-22",
  "scope": "G41 residual: layerwise RMS parity probes for MoE and quantized paths",
  "validation": {
    "tests": "38 passed, 2 skipped (4 probes total: MoE, INT8, INT4, CUDA-cuBLAS layerwise RMS — all within LAYER_PARITY_ATOL=1e-3 / RTOL=2e-2)",
    "coverage": "engine.layer_metrics traces embedding + per-layer + final-norm RMS; compared against torch forward-hook RMS with LAYER_PARITY_ATOL=1e-3 / RTOL=2e-2",
    "quantized": "same dequantized weights both sides -> standard tolerance applies",
    "cuda": "cuBLAS matmul path probed layerwise on real GPU (XINGCHENG_CPP_CUDA=1)"
  },
  "files": [
    "Standalone tools/local-model/tests/test_cpp_inference.py"
  ]
}
