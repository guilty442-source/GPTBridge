{
  "schema": "self-learning-curriculum-gates/v1",
  "date": "2026-09-22",
  "scope": "§2.7 星澄自主訓練 — items 1 (品質漂移＋退化探針) / 2 (合成上限) / 3 (SFT 課程選擇) / 9 (maturity 重測＋資源帳)",
  "files": [
    "Standalone tools/local-model/src/backend/services/xingcheng/infrastructure/native_transformer/self_learning.py",
    "Standalone tools/local-model/src/backend/services/xingcheng/infrastructure/native_transformer/self_learning_support.py"
  ],
  "note": "implementation landed under auto-commit 6d1e5287 (auto-commit swept the two files mid-edit together with a foreign worker's cpp_inference changes; content verified intact at HEAD before evidence write)",
  "new_policy_fields": {
    "max_synthetic_ratio": "§2.7-2 合成/自我生成資料比例上限 (0=off)；超上限整循環 blocked synthetic-ratio-exceeded，防自我放大",
    "synthetic_source_prefixes": "來源前綴白名單，預設 (synthetic, self-distillation)；統計入 pool.synthetic_* 與循環報告",
    "min_pool_avg_quality": "§2.7-1 品質漂移觸發 (0=off)；verified 池平均 quality_score 低於門檻 → blocked quality-drift",
    "curriculum_enabled": "§2.7-3 課程選擇；讀 model-maturity.json 認證層決定本循環 SFT 課程",
    "curriculum_intent_map": "課程→intent 白名單篩池；篩後空池 blocked course-dataset-empty",
    "post_upgrade_maturity_recheck": "§2.7-9 升級後重測 maturity 並記入報告/state.last_maturity_recheck；失敗記錄不暗改 active",
    "maturity_recheck_device": "重測裝置（預設 cpu）",
    "degradation_probe_enabled": "§2.7-1 能力退化探針觸發；資料未達 min_new_examples 時以套件 baseline_metrics 量測現役權重，退化且新例 ≥ degradation_min_examples → 降門檻觸發訓練（其餘閘門不變）",
    "degradation_probe_suite": "探針套件（預設 star-native-eval-dialogue-v1，用其 baseline_metrics 常數為對照）",
    "degradation_min_examples": "退化觸發仍要求的最少新例（預設 1；0 新例 → idle degradation-detected-insufficient-data，誠實記錄不訓練）",
    "dedup_enabled": "§2.7-2 去重（預設開啟）：(input_text,target_text) 全等對跨 scope 只留首見（scope 字典序，決定性），剔除數入 pool.deduped",
    "exclude_probe_values": "§2.7-2 汙染排除（預設開啟）：input/target 含 chat-foundation PROBE_VALUES（星火測試/QZ-88/13 + 29/6 × 7）者剔除，剔除數入 pool.contaminated_dropped；全剔光 → idle pool-empty-after-sanitize",
    "max_dataset_examples": "§2.7-8 資料上限（0=不設限）：進資料集總例數決定性截斷（scope 字典序→revision 升冪取前 N），截斷量入 summary.dataset_cap_truncated"
  },
  "degradation_probe_semantics": {
    "ordering": "只在 below-threshold 分支執行（已達門檻不浪費一次模型載入）",
    "fail_closed": "探針量測失敗／套件缺失／無現役權重 → degraded=False 不觸發，錯誤記錄於結果 degradation_probe 與 state.last_degradation_probe",
    "recording": "觸發時 summary.trigger=degradation-probe；閒置時結果附 degradation_probe 供稽核"
  },
  "curriculum_mapping": {
    "state_unreadable_or_missing": "blocked maturity-state-unavailable (fail-closed)",
    "certified<4": "blocked curriculum-foundation-unmet (對話基礎未達不得自主訓練)",
    "4": "sft-dialogue (target L5)",
    "5": "sft-reasoning (target L6)",
    "6": "sft-evolution (target L7)",
    ">=7": "sft-refresh (維持性刷新)"
  },
  "resource_account": "§2.7-9 每循環記錄 elapsed_seconds/steps/device/dataset_example_count/train_count/validation_count 入循環報告與 state.resource_account",
  "defaults": "全部新閘門預設關閉（0/False/固定 sft 課程），既有行為不變；啟用值屬受管 runtime/settings/self-learning.json",
  "verification": {
    "new_gate_tests": "tests/test_self_learning_gates.py 21/21 PASS（含 dedup 剔除/汙染剔除/全污閒置/資料上限截斷；註：xdist 並行下偶發 worker 記憶體壓力當機，單跑全過——主機共載所致非邏輯失敗）— 合成上限阻斷/通過、品質漂移阻斷、maturity 狀態缺失/基礎未達阻斷、課程依層選擇(L4→dialogue,L7→refresh)、intent 篩空池阻斷、資源帳記錄、退化探針觸發/無資料閒置/未退化閒置/錯誤不觸發/套件缺失/指標退化判定、dedup/汙染/資料上限、預設行為不變",
    "existing_regression": "test_phase_self_learning + test_self_learning_quiet_hours + test_training_job_executor + test_lifecycle_resource_actions + test_retention_weight_retirement 28/28 PASS",
    "syntax": "import check OK; SelfLearningPolicy dataclass fields verified at HEAD"
  },
  "gaps_remaining": [
    "§2.7-3 non-SFT course types (DPO/distill/續訓/專家模組)",
    "§2.7-8 resource-overspend auto-stop mid-training (資料上限已實作 max_dataset_examples；訓練中超支即停需 executor 內嵌預算計時，未實作)",
    "§2.7-7 rollback rehearsal — UNRESOLVED: _retire_previous_artifact prunes previous weights, conflicts with 保留上一代+rollback 實測; governor ruling needed",
    "§2.7-10 3 consecutive autonomous cycles acceptance + codex amendment"
  ]
}
